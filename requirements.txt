# CPU-only. Never install a CUDA build: the pipeline asserts torch.cuda is unused.
# Install torch from the CPU index:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

numpy>=1.26,<3
pandas>=2.0
pyarrow>=14.0
pyyaml>=6.0
scikit-learn>=1.4
scipy>=1.11
joblib>=1.3

torch>=2.2            # CPU wheel, see note above
lightgbm>=4.1         # SHAP surrogate
shap>=0.44
optuna>=3.5

matplotlib>=3.8       # viz.py only; train.py must not import it
requests>=2.31        # presigned GET/PUT, the only S3 access from Kaggle

pytest>=8.0
